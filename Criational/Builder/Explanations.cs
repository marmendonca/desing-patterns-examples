namespace DesingPatternsExamples.Criational.Builder;

public class Explanations
{
    // O padrão de projeto Builder é útil quando você precisa criar objetos complexos que possuem muitos parâmetros opcionais ou configuráveis.
    // Ele permite que você construa diferentes representações de um objeto, mantendo o mesmo processo de construção.
    
    // Prós
    // Você pode construir objetos passo a passo, adiar as etapas de construção ou rodar etapas recursivamente.
    // Você pode reutilizar o mesmo código de construção quando construindo várias representações de produtos.
    // Princípio de responsabilidade única. Você pode isolar um código de construção complexo da lógica de negócio do produto.
    
    // Contras
    // A complexidade geral do código aumenta uma vez que o padrão exige criar múltiplas classes novas.
}